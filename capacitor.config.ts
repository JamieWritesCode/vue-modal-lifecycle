import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'modal-lifecycle',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
